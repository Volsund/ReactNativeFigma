import React from 'react';
import { storiesOf } from '@storybook/react-native';
import { ProfileScreen } from '../../../src/views';

storiesOf('ProfileScreen', module).add('default', () => <ProfileScreen />);
